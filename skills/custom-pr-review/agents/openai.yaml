interface:
  display_name: "Custom PR Review"
  short_description: "Review PRs for simplicity and root-cause fixes"
  default_prompt: "Use $custom-pr-review to review this branch or pull request against the default branch."
